<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Kiosk-only overrides: start with the bars already gone so launch
        doesn't flash a status bar before KioskController hides them.
        The normal build keeps the plain Light/NoActionBar theme.
    -->
    <style name="Theme.Parrot" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
